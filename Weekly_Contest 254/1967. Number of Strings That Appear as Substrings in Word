class Solution {
    public int numOfStrings(String[] p, String w) 
    {
        int num=0;
        for(int i=0;i<p.length;i++)
        {
            if(w.contains(p[i]))
            {
                num++;
            }
        }
        return num;
    }
}
